import { createMuiTheme } from '@material-ui/core/styles'

const theme = createMuiTheme({
  palette: {
    primary: {
      light: '#acd434',
      main: '#98CA02',
      dark: '#6a8d01',
      contrastText: '#000'
    }
  }
})

export default theme
